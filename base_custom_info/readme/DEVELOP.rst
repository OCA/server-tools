To create a module that supports custom information, just depend on this module
and inherit from the ``custom.info`` model.

See examples in the ``product_custom_info`` addon in the ``product-attribute`` 
repository and in the ``website_sale_custom_info`` addon mentionned earlier in 
the *Category* section.
