<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Security Groups -->
    <record id="group_encryption_auditor" model="res.groups">
        <field name="name">Encryption Auditor</field>
        <field name="comment">Can view audit logs to review who accessed encrypted data</field>
        <field name="category_id" ref="base.module_category_hidden"/>
    </record>

    <record id="group_encryption_admin" model="res.groups">
        <field name="name">Encryption Administrator</field>
        <field name="comment">Can rotate keys and perform encryption migrations</field>
        <field name="category_id" ref="base.module_category_hidden"/>
        <field name="implied_ids" eval="[(4, ref('group_encryption_auditor'))]"/>
        <field name="users" eval="[(4, ref('base.user_admin'))]"/>
    </record>

    <!-- Record Rules -->
    <record id="rule_audit_log_auditor" model="ir.rule">
        <field name="name">Audit Log: Auditors see all</field>
        <field name="model_id" ref="model_pb_encrypted_audit_log"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('group_encryption_auditor'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <record id="rule_audit_log_admin" model="ir.rule">
        <field name="name">Audit Log: Admin full access</field>
        <field name="model_id" ref="model_pb_encrypted_audit_log"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('group_encryption_admin'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>
</odoo>
