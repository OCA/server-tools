This module instruments Odoo with OpenTelemetry support, including `traceparent`
support for distributed tracing and correlation. It also includes optional support for
instrumenting PostgreSQL (via psycopg2).
