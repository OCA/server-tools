This module must be added to the `server_wide_modules` in your Odoo configuration. You
must also specify a `db_name` in the config, e.g., `db_name = FirstDB,AnotherDB` - the
module **will not work** in multi-DB mode.