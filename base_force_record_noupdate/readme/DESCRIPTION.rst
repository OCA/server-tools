When installed this module allows configuring a list of models that should be protected from overwrite during an update of addons in the settings.
The module is useful when you have a model that you want to keep the data of during an update of the module.
