<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_conditional_image_tree" model="ir.ui.view">
        <field name="name">conditional.image.tree</field>
        <field name="model">conditional.image</field>
        <field name="arch" type="xml">
            <tree>
                <field name="sequence" widget="handle" />
                <field name="default" />
                <field name="name" />
                <field name="model_id" />
                <field name="model_name" invisible="1" />
                <field name="selector" />
            </tree>
        </field>
    </record>

    <record id="view_conditional_image_form" model="ir.ui.view">
        <field name="name">conditional.image.form</field>
        <field name="model">conditional.image</field>
        <field name="arch" type="xml">
            <form string="Conditional Image">
                <sheet>
                    <group>
                        <group>
                            <field name="name" />
                            <field name="model_id" />
                            <field name="model_name" invisible="1" />
                            <field name="default" />
                        </group>
                        <group>
                            <field
                                name="image_1920"
                                widget='image'
                                class="oe_avatar"
                                options='{"preview_image": "image_1920"}'
                            />
                        </group>
                        <group
                            string="Selector"
                            attrs="{'invisible': [('default', '=', True)]}"
                        >
                            <field
                                name="selector"
                                nolabel="True"
                                widget="ace"
                                options="{'mode': 'python'}"
                            />
                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" />
                    <field name="message_ids" />
                </div>
            </form>
        </field>
    </record>

    <record id="view_conditional_image_search" model="ir.ui.view">
        <field name="name">conditional.image.search</field>
        <field name="model">conditional.image</field>
        <field name="arch" type="xml">
            <search string="Conditional Image">
                <field name="name" />
                <field name="model_id" />
                <field name="default" />
                <separator />
                <filter
                    string="Archived"
                    name="inactive"
                    domain="[('active', '=', False)]"
                />
                <group
                    expand="0"
                    string="Group By"
                    colspan="11"
                    col="11"
                    groups="base.group_no_one"
                >
                    <filter
                        string="Default"
                        name="group_by_default"
                        domain="[]"
                        context="{'group_by': 'default'}"
                    />
                    <filter
                        string="Model"
                        name="group_by_model"
                        domain="[]"
                        context="{'group_by': 'model_id'}"
                    />
                </group>
            </search>
        </field>
    </record>

    <record id="conditional_image_action" model="ir.actions.act_window">
        <field name="name">Conditional Images</field>
        <field name="res_model">conditional.image</field>
        <field name="view_mode">tree,form</field>
    </record>

    <record model="ir.ui.menu" id="conditional_image_menu">
        <field name="name">Conditional Images</field>
        <field name="parent_id" ref="base.menu_ir_property" />
        <field name="action" ref="conditional_image_action" />
        <field name="sequence" eval="99" />
    </record>
</odoo>
