# Use the configparse.ExtendedInterpolation class to interpolate over sections.
[login]

# Test values (commit these)
# database = hc16-test-labdatabase
# base_url = https://hc16-test-lab.therp1.nl

# Local values (uncomment locally)
database = odohumdem16
base_url = http://localhost:8069

# Fixed values
url_login = ${base_url}/web/session/authenticate
username = api_user

[template_get]
endpoint = ${login:base_url}/servertools/template-get

[template_put]
endpoint = ${login:base_url}/servertools/template-put
